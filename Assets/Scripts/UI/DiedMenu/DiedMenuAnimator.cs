using TMPro;
using UnityEngine;
using UnityEngine.UI;

namespace UI.DiedMenu
{
    public class DiedMenuAnimator : MonoBehaviour
    {
        [SerializeField] private Image _panel, _restartButton, _rebornButton;
        [SerializeField] private TMP_Text _score, _diedText;

        private void OnEnable()
        {
            
        }

        private void OnDisable()
        {
            
        }
    }
}